= <%= user_name %> finished deployment of branch <%= branch %> to <%= stage %>:[<%= hosts %>] for <%= application %> (sun)

Compare:
<%= git_compare_prefix %>/<%= git_compare_params %>

Commits:
<%= git_log %>
